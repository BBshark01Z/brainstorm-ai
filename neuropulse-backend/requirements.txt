# NeuroPulse Backend — Python dependencies (pinned to tested versions).
# Tested locally with Python 3.13.0 in venv_new.
# NOTE: scikit-learn + python-multipart are imported by the app but were not
# installed in the working local venv_new; their pins come from the project's
# other venv (3.13.15) so the deployed app has full functionality
# (sklearn model load + OAuth2 form parsing).

fastapi==0.141.1
uvicorn[standard]==0.52.1
numpy==2.5.2
scipy==1.18.0
mne==1.12.1
pydantic==2.13.4
httpx==0.28.1
python-dotenv==1.2.2
websockets==17.0.1
bcrypt==5.0.0
python-jose[cryptography]==3.5.0
python-multipart==0.0.32
joblib==1.5.3
scikit-learn==1.9.0